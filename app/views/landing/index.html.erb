<h2>Welcome message or signature here</h2>
<% @paintings.each do |painting| %>
  <p><%= painting.title %></p>
  <p><%= painting.price %></p>
  <img src="<%= asset_path painting.image %>" alt="<%= painting.title %>">
  <p><%= painting.description %></p>
<% end %>
